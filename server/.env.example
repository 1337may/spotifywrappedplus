# Client ID/Secret are no longer set here — each visitor enters their own
# on the login page (see README). These vars are optional local-dev overrides.
REDIRECT_URI=http://127.0.0.1:5173/callback
CLIENT_URL=http://127.0.0.1:5173
PORT=8888
